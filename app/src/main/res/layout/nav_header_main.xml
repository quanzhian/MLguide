<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="@drawable/side_nav_bar"
    android:gravity="center"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">
    <!-- android:theme="@style/ThemeOverlay.AppCompat.Dark" -->

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="@dimen/nav_header_head_size"
        android:layout_height="@dimen/nav_header_head_size"
        android:layout_centerInParent="true"
        android:src="@mipmap/default_head_img" />

    <TextView
        android:id="@+id/nav_header_user_name"
        android:layout_below="@+id/imageView"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/nav_header_vertical_spacing"
        android:textAppearance="@style/style_textview_one"
        android:text="昵称" />

    <!-- @style/TextAppearance.AppCompat.Body1 -->
    <TextView
        android:id="@+id/textView"
        android:layout_below="@+id/nav_header_user_name"
        android:layout_centerInParent="true"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/nav_header_vertical_spacing"
        android:textAppearance="@style/style_textview_one"
        android:text="我就是我,颜色不一样的烟火" />

</RelativeLayout>
